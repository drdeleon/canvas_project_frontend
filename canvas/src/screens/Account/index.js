import React, { Fragment } from 'react';
import { connect } from 'react-redux';

const Account = ({}) => {

    return (
        <> 
        <h1> Account </h1>
        </>
    );
};

export default connect(
    state => ({}),
    dispatch => ({}),
    )(Account);