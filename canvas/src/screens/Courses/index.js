import React, { Fragment } from 'react';
import { connect } from 'react-redux';

const Courses = ({}) => {

    return (
        <> 
        <h1> Courses </h1>
        </>
    );
};

export default connect(
    state => ({}),
    dispatch => ({}),
    )(Courses);