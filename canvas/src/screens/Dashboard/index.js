import React, { Fragment } from 'react';
import { connect } from 'react-redux';

const Dashboard = ({}) => {

    return (
        <Fragnment> 
        <h1> Dashboard </h1>
        </Fragnment>
    );
};

export default connect(
    state => ({}),
    dispatch => ({}),
    )(Dashboard);